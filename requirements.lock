# Phase 2 Kaggle bootstrap lock (Python 3.12).
#
# Kaggle's accelerator image supplies the CUDA build of PyTorch and NumPy. The
# notebook records and checks those platform packages rather than reinstalling
# them, which would risk replacing the working CUDA build. Later A2 phases extend
# this lock when OCR/embedding/index packages are selected by experiment.
pydantic==2.12.3
pydantic-settings==2.15.0
PyMuPDF==1.26.7
Pillow==11.3.0
PyYAML==6.0.3
pytest==8.4.2
ruff==0.16.2
black==26.5.1
